<h1>Favorite Questions</h1>

<% @questions.each do |question| %>
<p>
<%= link_to "#{question.title}", question_path(question)%>
<%#= link_to (raw("<span class='glyphicon glyphicon-remove' style='font-size:12px;'></span>")), question_favorite_path(question, question.user_id), method: :delete %>
</p>
<% end %>
