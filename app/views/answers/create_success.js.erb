// j is a helper methdo from Rails to escape the JS so it doesn't break with quotes or new line characters
$('.answers').prepend('<%= j render "answer", answer: @answer %>')
// clears the input field for creating an answer
// $('#answer_body').val("");

// create a new answer because it persisted from the previous if there were errors; but we want to create a new answer
// rather than update existing
<% @answer = Answer.new %>
$("#answer-form").html("<%= j render 'form' %>");
